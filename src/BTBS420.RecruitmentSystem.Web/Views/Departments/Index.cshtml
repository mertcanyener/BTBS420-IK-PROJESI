@model BTBS420.RecruitmentSystem.Web.ViewModels.Departments.DepartmentIndexViewModel

@{
    ViewData["Title"] = "Departmanlar";
}

<div class="d-flex flex-column flex-md-row align-items-md-center justify-content-between gap-3 mb-4">
    <h1 class="h2 mb-0">Departmanlar</h1>
    <a class="btn btn-primary" asp-controller="Departments" asp-action="Create">Yeni Departman</a>
</div>

@if (Model.Departments.Count == 0)
{
    <div class="card border-0 shadow-sm">
        <div class="card-body p-4 text-center">
            <p class="text-body-secondary mb-0">Henüz departman eklenmedi.</p>
        </div>
    </div>
}
else
{
    <table class="table align-middle">
        <thead>
            <tr>
                <th scope="col">Ad</th>
                <th scope="col">Durum</th>
                <th scope="col" class="text-end">İşlemler</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var department in Model.Departments)
            {
                <tr>
                    <td>@department.Name</td>
                    <td>
                        @if (department.IsActive)
                        {
                            <span class="badge text-bg-success">Aktif</span>
                        }
                        else
                        {
                            <span class="badge text-bg-secondary">Pasif</span>
                        }
                    </td>
                    <td class="text-end">
                        <a class="btn btn-sm btn-outline-primary" asp-controller="Departments" asp-action="Edit" asp-route-id="@department.Id">Düzenle</a>
                        @if (department.IsActive)
                        {
                            <form asp-controller="Departments" asp-action="Deactivate" asp-route-id="@department.Id" method="post" asp-antiforgery="true" class="d-inline">
                                <button type="submit" class="btn btn-sm btn-outline-secondary">Pasife Al</button>
                            </form>
                        }
                        else
                        {
                            <form asp-controller="Departments" asp-action="Activate" asp-route-id="@department.Id" method="post" asp-antiforgery="true" class="d-inline">
                                <button type="submit" class="btn btn-sm btn-outline-success">Aktifleştir</button>
                            </form>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
