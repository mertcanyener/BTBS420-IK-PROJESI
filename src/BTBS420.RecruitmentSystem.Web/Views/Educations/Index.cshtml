@model BTBS420.RecruitmentSystem.Web.ViewModels.Educations.EducationIndexViewModel

@{
    ViewData["Title"] = "Eğitimler";
}

<div class="d-flex flex-column flex-md-row align-items-md-center justify-content-between gap-3 mb-4">
    <h1 class="h2 mb-0">Eğitimler</h1>
    <a class="btn btn-primary" asp-controller="Educations" asp-action="Create">Yeni Eğitim</a>
</div>

@if (Model.Educations.Count == 0)
{
    <div class="card border-0 shadow-sm">
        <div class="card-body p-4 text-center">
            <p class="text-body-secondary mb-0">Henüz eğitim eklenmedi.</p>
        </div>
    </div>
}
else
{
    <table class="table align-middle">
        <thead>
            <tr>
                <th scope="col">Ad</th>
                <th scope="col">Durum</th>
                <th scope="col" class="text-end">İşlemler</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var education in Model.Educations)
            {
                <tr>
                    <td>@education.Name</td>
                    <td>
                        @if (education.IsActive)
                        {
                            <span class="badge text-bg-success">Aktif</span>
                        }
                        else
                        {
                            <span class="badge text-bg-secondary">Pasif</span>
                        }
                    </td>
                    <td class="text-end">
                        <a class="btn btn-sm btn-outline-primary" asp-controller="Educations" asp-action="Edit" asp-route-id="@education.Id">Düzenle</a>
                        @if (education.IsActive)
                        {
                            <form asp-controller="Educations" asp-action="Deactivate" asp-route-id="@education.Id" method="post" asp-antiforgery="true" class="d-inline">
                                <button type="submit" class="btn btn-sm btn-outline-secondary">Pasife Al</button>
                            </form>
                        }
                        else
                        {
                            <form asp-controller="Educations" asp-action="Activate" asp-route-id="@education.Id" method="post" asp-antiforgery="true" class="d-inline">
                                <button type="submit" class="btn btn-sm btn-outline-success">Aktifleştir</button>
                            </form>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
