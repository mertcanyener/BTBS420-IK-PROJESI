@model BTBS420.RecruitmentSystem.Web.ViewModels.ExperienceRanges.ExperienceRangeIndexViewModel

@{
    ViewData["Title"] = "Deneyim Aralıkları";
}

<div class="d-flex flex-column flex-md-row align-items-md-center justify-content-between gap-3 mb-4">
    <h1 class="h2 mb-0">Deneyim Aralıkları</h1>
    <a class="btn btn-primary" asp-controller="ExperienceRanges" asp-action="Create">Yeni Deneyim Aralığı</a>
</div>

@if (Model.ExperienceRanges.Count == 0)
{
    <div class="card border-0 shadow-sm">
        <div class="card-body p-4 text-center">
            <p class="text-body-secondary mb-0">Henüz deneyim aralığı eklenmedi.</p>
        </div>
    </div>
}
else
{
    <table class="table align-middle">
        <thead>
            <tr>
                <th scope="col">Ad</th>
                <th scope="col">Min (Yıl)</th>
                <th scope="col">Max (Yıl)</th>
                <th scope="col">Durum</th>
                <th scope="col" class="text-end">İşlemler</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var experienceRange in Model.ExperienceRanges)
            {
                <tr>
                    <td>@experienceRange.Name</td>
                    <td>@experienceRange.MinYears</td>
                    <td>@experienceRange.MaxYears</td>
                    <td>
                        @if (experienceRange.IsActive)
                        {
                            <span class="badge text-bg-success">Aktif</span>
                        }
                        else
                        {
                            <span class="badge text-bg-secondary">Pasif</span>
                        }
                    </td>
                    <td class="text-end">
                        <a class="btn btn-sm btn-outline-primary" asp-controller="ExperienceRanges" asp-action="Edit" asp-route-id="@experienceRange.Id">Düzenle</a>
                        @if (experienceRange.IsActive)
                        {
                            <form asp-controller="ExperienceRanges" asp-action="Deactivate" asp-route-id="@experienceRange.Id" method="post" asp-antiforgery="true" class="d-inline">
                                <button type="submit" class="btn btn-sm btn-outline-secondary">Pasife Al</button>
                            </form>
                        }
                        else
                        {
                            <form asp-controller="ExperienceRanges" asp-action="Activate" asp-route-id="@experienceRange.Id" method="post" asp-antiforgery="true" class="d-inline">
                                <button type="submit" class="btn btn-sm btn-outline-success">Aktifleştir</button>
                            </form>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
